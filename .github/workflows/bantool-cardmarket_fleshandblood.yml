name: fleshandblood / cardmarket

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [cardmarket_fleshandblood]
  schedule:
    - cron: "20 4,16 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-cardmarket
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: cardmarket_fleshandblood
      game: "fleshandblood"
      datastore-filepath: b2://mtgban-datastore/fleshandblood/fleshandblood.json.xz
      mkmids-filepath: b2://mtgban-datastore/fleshandblood/cardmarket_catalog.json.xz
    secrets: inherit
