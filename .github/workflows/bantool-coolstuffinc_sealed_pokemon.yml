name: pokemon / coolstuffinc_sealed

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [coolstuffinc_sealed_pokemon]
  schedule:
    - cron: "27 9,21 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-coolstuffinc
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: coolstuffinc_sealed_pokemon
      game: "pokemon"
      datastore-filepath: b2://mtgban-datastore/pokemon/pokemon.json.xz
    secrets: inherit
