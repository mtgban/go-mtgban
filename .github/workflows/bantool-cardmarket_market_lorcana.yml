name: lorcana / cardmarket_market

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [cardmarket_market_lorcana]
  schedule:
    - cron: "0 7 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-cardmarket
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: cardmarket_market_lorcana
      game: "lorcana"
      datastore-filepath: b2://mtgban-datastore/lorcana/lorcana.json.xz
      mkmids-filepath: b2://mtgban-datastore/lorcana/cardmarket_catalog.json.xz
    secrets: inherit
