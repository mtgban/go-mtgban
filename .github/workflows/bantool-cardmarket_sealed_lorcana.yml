name: lorcana / cardmarket_sealed

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [cardmarket_sealed_lorcana]
  schedule:
    - cron: "5 */12 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-cardmarket
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: cardmarket_sealed_lorcana
      game: "lorcana"
      datastore-filepath: b2://mtgban-datastore/lorcana/lorcana.json.xz
    secrets: inherit
