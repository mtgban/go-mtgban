name: onepiece / coolstuffinc_sealed

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [coolstuffinc_sealed_onepiece]
  schedule:
    - cron: "31 1,13 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-coolstuffinc
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: coolstuffinc_sealed_onepiece
      game: "onepiece"
      datastore-filepath: b2://mtgban-datastore/onepiece/onepiece.json.xz
    secrets: inherit
