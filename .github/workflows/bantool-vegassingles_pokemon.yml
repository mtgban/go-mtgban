name: pokemon / vegassingles

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [vegassingles_pokemon]
  schedule:
    - cron: "9 20 * * *"

permissions:
  contents: read

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: vegassingles_pokemon
      game: "pokemon"
      datastore-filepath: b2://mtgban-datastore/pokemon/pokemon.json.xz
    secrets: inherit
