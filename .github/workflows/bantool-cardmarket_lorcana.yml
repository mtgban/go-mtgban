name: lorcana / cardmarket

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [cardmarket_lorcana]
  schedule:
    - cron: "13 2,14 * * *"

permissions:
  contents: read

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: cardmarket_lorcana
      game: "lorcana"
      affiliate: ${{ vars.MKM_PARTNER }}
      datastore-filepath: b2://mtgban-datastore/lorcana/lorcana.json.xz
      mkmids-filepath: b2://mtgban-datastore/lorcana/cardmarket_catalog.json.xz
    secrets: inherit
