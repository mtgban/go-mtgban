name: palworld / tcg_sealed

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [tcg_sealed_palworld]
  schedule:
    - cron: "10 10,22 * * *"

permissions:
  contents: read

concurrency:
  group: bantool-tcgplayer
  cancel-in-progress: false
  queue: max

jobs:
  deploy:
    name: ${{ github.workflow }}
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: tcg_sealed_palworld
      game: "palworld"
      datastore-filepath: b2://mtgban-datastore/palworld/palworld.json.xz
    secrets: inherit
