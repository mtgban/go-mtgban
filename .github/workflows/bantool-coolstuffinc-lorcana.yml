name: coolstuffinc_lorcana

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [coolstuffinc_lorcana]
  schedule:
    - cron: "0 */12 * * *"

jobs:
  cache-datastore:
    uses: ./.github/workflows/cache-file.yml
    with:
      url: ${{ vars.DATASTORE_LORCANA }}
      path: "datastore/"

  deploy:
    name: ${{ github.workflow }}
    needs: [cache-datastore]
    uses: ./.github/workflows/run-bantool.yml
    with:
      target: ${{ github.workflow }}
      game: "lorcana"
      datastore-filepath: ${{ needs.cache-datastore.outputs.filepath }}
      datastore-cache-key: ${{ needs.cache-datastore.outputs.key }}
    secrets: inherit
